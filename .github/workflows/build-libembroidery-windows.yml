name: build Windows

on:
  workflow_dispatch:
  schedule:
    - cron: "15 6 * * *"

jobs:
  build-windows-latest:
    runs-on: windows-latest
    steps:
      - name: build
        run:
          git clone http://github.com/embroidermodder/libembroidery
          cmake libembroidery
          cmake --build .
